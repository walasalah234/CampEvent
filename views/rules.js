import React from 'react';
import {View, Text} from 'react-native';
import constants from '../constants';

export default () => (
    <View>
        <Text>
            {constants.RULES}
        </Text>
    </View>
)